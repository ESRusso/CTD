function polegadasEmCentimetro(polegadas) {
    return polegadas * 2.54;
}

console.log(polegadasEmCentimetro(10))


function converter(polegadas) {
    return polegadas * 2.54;
}

console.log(converter(10))

// Recebe pelo parametro polegada
// Retorna seu equivalente em centimetros